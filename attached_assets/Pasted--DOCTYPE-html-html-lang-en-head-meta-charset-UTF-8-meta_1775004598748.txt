<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Orders - Som Tam Thai</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.development.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js" crossorigin></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        /* Task 1-0.1: Design Tokens & Theme File */
        :root {
            --bg-cream: #FDFBF7;
            --primary-green: #1A4D2E;
            --accent-terracotta: #E36414;
            --highlight-gold: #FFB703;
        }
        @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Lora:ital,wght@0,400;0,600;1,400&display=swap');
        
        body { 
            background-color: var(--bg-cream); 
            font-family: 'DM Sans', sans-serif; 
            color: #333;
        }
        .display-font { 
            font-family: 'Lora', serif; 
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: {
                            cream: '#FDFBF7',
                            green: '#1A4D2E',
                            terracotta: '#E36414',
                            gold: '#FFB703'
                        }
                    }
                }
            }
        }
    </script>
</head>
<body className="antialiased">
    <div id="root"></div>

    <script type="text/babel">
        const { ShoppingBag, ChevronDown, ChevronUp, Clock, MapPin, RotateCcw, Package, Check, CircleDot, Circle } = lucide;

        // Dummy Data
        const ACTIVE_ORDER = {
            id: "#STT-20260401-0042",
            date: "Today, 18:45",
            statusIndex: 2, // 0: Received, 1: Confirmed, 2: Preparing, 3: Ready, 4: Out for Delivery, 5: Completed
            itemsSummary: "2x Authentic Som Tam, 1x Pad Thai",
            total: 496.80,
            eta: "19:30 - 19:45"
        };

        const PAST_ORDERS = [
            {
                id: "#STT-20260315-0018",
                date: "15 Mar 2026",
                itemsSummary: "1x Green Curry Chicken, 2x Jasmine Rice, 1x Thai Iced Tea",
                total: 285.00,
                status: "Completed",
                items: [
                    { name: "Green Curry Chicken", qty: 1, price: 170 },
                    { name: "Extra Jasmine Rice", qty: 2, price: 50 },
                    { name: "Thai Iced Tea", qty: 1, price: 65 }
                ]
            },
            {
                id: "#STT-20260228-0056",
                date: "28 Feb 2026",
                itemsSummary: "2x Tom Yum Goong, 1x Pad Kra Pao",
                total: 510.00,
                status: "Completed",
                items: [
                    { name: "Tom Yum Goong", qty: 2, price: 360 },
                    { name: "Pad Kra Pao", qty: 1, price: 150 }
                ]
            },
            {
                id: "#STT-20260110-0005",
                date: "10 Jan 2026",
                itemsSummary: "1x Mango Sticky Rice",
                total: 110.00,
                status: "Cancelled",
                items: [
                    { name: "Mango Sticky Rice", qty: 1, price: 110 }
                ]
            }
        ];

        // Reused NavBar Component
        const NavBar = () => (
            <nav className="sticky top-0 z-50 bg-white shadow-sm px-4 py-3 flex justify-between items-center border-b border-gray-100">
                <div className="flex items-center gap-2">
                    <span className="display-font font-bold text-xl text-brand-green">Som Tam Thai</span>
                </div>
                <div className="hidden md:flex gap-6 font-medium text-sm">
                    <a href="index.html" className="text-gray-500 hover:text-brand-terracotta transition-colors">Menu</a>
                    <a href="reservations.html" className="hover:text-brand-terracotta transition-colors">Reservations</a>
                    <a href="orders.html" className="text-brand-terracotta border-b-2 border-brand-terracotta pb-1">Orders</a>
                    <a href="account.html" className="hover:text-brand-terracotta transition-colors">Account</a>
                </div>
                <div className="flex items-center gap-4">
                    <a href="cart.html" className="relative p-2 text-gray-500 hover:text-brand-green">
                        <i data-lucide="shopping-bag" className="w-6 h-6"></i>
                    </a>
                </div>
            </nav>
        );

        // Task 1-4.4: Order Status Tracker Component
        const OrderStatusTracker = ({ currentStepIndex }) => {
            const steps = [
                { label: "Received", time: "18:45" },
                { label: "Confirmed", time: "18:47" },
                { label: "Preparing", time: "18:50" },
                { label: "Ready", time: "--:--" },
                { label: "Out for Delivery", time: "--:--" },
                { label: "Completed", time: "--:--" }
            ];

            return (
                <div className="py-4 pl-4 relative">
                    {/* Vertical connecting line */}
                    <div className="absolute left-7 top-8 bottom-8 w-0.5 bg-gray-200"></div>
                    
                    <div className="space-y-6 relative z-10">
                        {steps.map((step, idx) => {
                            const isCompleted = idx < currentStepIndex;
                            const isActive = idx === currentStepIndex;
                            const isFuture = idx > currentStepIndex;

                            return (
                                <div key={idx} className={`flex items-start ${isFuture ? 'opacity-50' : 'opacity-100'}`}>
                                    <div className="flex-shrink-0 mr-4 relative bg-white">
                                        {isCompleted ? (
                                            <div className="w-6 h-6 rounded-full bg-brand-green text-white flex items-center justify-center">
                                                <i data-lucide="check" className="w-3.5 h-3.5"></i>
                                            </div>
                                        ) : isActive ? (
                                            <div className="relative flex h-6 w-6 items-center justify-center">
                                                <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-brand-green opacity-40"></span>
                                                <div className="relative w-6 h-6 rounded-full bg-white border-2 border-brand-green flex items-center justify-center">
                                                    <div className="w-2 h-2 rounded-full bg-brand-green"></div>
                                                </div>
                                            </div>
                                        ) : (
                                            <div className="w-6 h-6 rounded-full bg-white border-2 border-gray-300"></div>
                                        )}
                                    </div>
                                    <div className="flex-1 -mt-1">
                                        <h4 className={`text-sm font-bold ${isActive ? 'text-brand-green' : 'text-gray-900'}`}>{step.label}</h4>
                                        <p className="text-xs text-gray-500">{isCompleted || isActive ? step.time : 'Pending'}</p>
                                    </div>
                                </div>
                            );
                        })}
                    </div>
                </div>
            );
        };

        // Expandable Row for Past Orders
        const OrderHistoryRow = ({ order }) => {
            const [isExpanded, setIsExpanded] = React.useState(false);
            const isCancelled = order.status === "Cancelled";

            return (
                <div className="border border-gray-100 rounded-lg mb-3 bg-white overflow-hidden shadow-sm hover:shadow transition-shadow">
                    {/* Header Row (Always Visible) */}
                    <div 
                        className="p-4 sm:p-5 flex flex-col sm:flex-row justify-between items-start sm:items-center cursor-pointer gap-4"
                        onClick={() => setIsExpanded(!isExpanded)}
                    >
                        <div className="flex-1">
                            <div className="flex items-center gap-3 mb-1">
                                <span className="font-bold text-gray-900">{order.id}</span>
                                <span className={`text-[10px] font-bold uppercase tracking-wider px-2 py-0.5 rounded-full ${isCancelled ? 'bg-red-50 text-red-600' : 'bg-gray-100 text-gray-600'}`}>
                                    {order.status}
                                </span>
                            </div>
                            <div className="text-sm text-gray-500 mb-1">{order.date}</div>
                            <div className="text-sm font-medium text-gray-700 line-clamp-1">{order.itemsSummary}</div>
                        </div>
                        
                        <div className="flex items-center justify-between w-full sm:w-auto gap-6 sm:gap-4 border-t border-gray-100 pt-3 sm:border-0 sm:pt-0">
                            <div className="font-bold text-brand-green">MVR {order.total.toFixed(2)}</div>
                            <button className="text-gray-400 hover:text-brand-green transition-colors p-1">
                                {isExpanded ? <i data-lucide="chevron-up" className="w-5 h-5"></i> : <i data-lucide="chevron-down" className="w-5 h-5"></i>}
                            </button>
                        </div>
                    </div>

                    {/* Expandable Details Content */}
                    {isExpanded && (
                        <div className="px-4 sm:px-5 pb-5 pt-2 border-t border-gray-100 bg-gray-50 animate-in slide-in-from-top-2">
                            <h4 className="text-xs font-bold text-gray-500 uppercase tracking-wider mb-3">Order Details</h4>
                            <div className="space-y-2 mb-4">
                                {order.items.map((item, idx) => (
                                    <div key={idx} className="flex justify-between text-sm">
                                        <span className="text-gray-700">{item.qty}x {item.name}</span>
                                        <span className="text-gray-900 font-medium">MVR {item.price.toFixed(2)}</span>
                                    </div>
                                ))}
                            </div>
                            
                            <div className="flex justify-between items-center pt-4 border-t border-gray-200 mt-4">
                                <a href="cart.html" className="inline-flex items-center text-sm font-bold text-brand-terracotta hover:text-orange-700 transition-colors bg-orange-50 px-3 py-1.5 rounded-lg">
                                    <i data-lucide="rotate-ccw" className="w-4 h-4 mr-1.5"></i> Reorder Items
                                </a>
                                <div className="text-right">
                                    <div className="text-xs text-gray-500">Delivery Fee included</div>
                                </div>
                            </div>
                        </div>
                    )}
                </div>
            );
        };

        // Main App Component
        const App = () => {
            React.useEffect(() => {
                lucide.createIcons();
            });

            return (
                <div className="min-h-screen flex flex-col bg-brand-cream relative">
                    <NavBar />

                    <main className="flex-grow max-w-5xl mx-auto w-full px-4 py-8">
                        <div className="mb-8">
                            <h1 className="display-font text-3xl md:text-4xl font-bold text-gray-900">Your Orders</h1>
                            <p className="text-gray-500 mt-2">Track active deliveries and view your past order history.</p>
                        </div>

                        <div className="flex flex-col lg:flex-row gap-8 lg:gap-12">
                            
                            {/* Left Column: Active Orders */}
                            <div className="w-full lg:w-2/5">
                                <h2 className="text-xl font-bold text-gray-900 mb-4 flex items-center">
                                    <span className="w-2 h-2 rounded-full bg-brand-green mr-2 animate-pulse"></span> Active Order
                                </h2>
                                
                                {ACTIVE_ORDER ? (
                                    <div className="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
                                        <div className="bg-brand-green p-4 text-white flex justify-between items-center">
                                            <div>
                                                <div className="text-xs text-brand-cream/70 uppercase tracking-wider font-bold mb-0.5">Order ID</div>
                                                <div className="font-bold">{ACTIVE_ORDER.id}</div>
                                            </div>
                                            <div className="text-right">
                                                <div className="text-xs text-brand-cream/70 uppercase tracking-wider font-bold mb-0.5">ETA</div>
                                                <div className="font-bold">{ACTIVE_ORDER.eta}</div>
                                            </div>
                                        </div>
                                        
                                        <div className="p-5">
                                            <div className="mb-6 pb-6 border-b border-gray-100">
                                                <OrderStatusTracker currentStepIndex={ACTIVE_ORDER.statusIndex} />
                                            </div>
                                            
                                            <div>
                                                <h4 className="text-xs font-bold text-gray-500 uppercase tracking-wider mb-2">Order Summary</h4>
                                                <p className="text-sm font-medium text-gray-700 mb-1">{ACTIVE_ORDER.itemsSummary}</p>
                                                <p className="text-sm font-bold text-brand-green">MVR {ACTIVE_ORDER.total.toFixed(2)}</p>
                                            </div>
                                        </div>
                                    </div>
                                ) : (
                                    <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-8 text-center flex flex-col items-center">
                                        <div className="w-16 h-16 bg-gray-50 rounded-full flex items-center justify-center mb-4">
                                            <i data-lucide="package" className="w-8 h-8 text-gray-400"></i>
                                        </div>
                                        <h3 className="font-bold text-gray-900 mb-1">No Active Orders</h3>
                                        <p className="text-sm text-gray-500">You don't have any orders currently in progress.</p>
                                    </div>
                                )}
                            </div>

                            {/* Right Column: Order History */}
                            <div className="w-full lg:w-3/5">
                                <h2 className="text-xl font-bold text-gray-900 mb-4">Order History</h2>
                                
                                {PAST_ORDERS.length > 0 ? (
                                    <div className="space-y-3">
                                        {PAST_ORDERS.map(order => (
                                            <OrderHistoryRow key={order.id} order={order} />
                                        ))}
                                    </div>
                                ) : (
                                    <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-8 text-center">
                                        <p className="text-gray-500">Your past orders will appear here.</p>
                                    </div>
                                )}
                            </div>

                        </div>
                    </main>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
