<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Management - Admin - Som Tam Thai</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.development.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js" crossorigin></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        /* Design Tokens & Theme File */
        :root {
            --bg-cream: #FDFBF7;
            --primary-green: #1A4D2E;
            --accent-terracotta: #E36414;
            --highlight-gold: #FFB703;
        }
        @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Lora:ital,wght@0,400;0,600;1,400&display=swap');
        
        body { 
            background-color: #f3f4f6; 
            font-family: 'DM Sans', sans-serif; 
            color: #333;
        }
        .display-font { font-family: 'Lora', serif; }
        .kanban-scroll::-webkit-scrollbar {
            height: 8px;
        }
        .kanban-scroll::-webkit-scrollbar-track {
            background: #e5e7eb;
            border-radius: 4px;
        }
        .kanban-scroll::-webkit-scrollbar-thumb {
            background: #9ca3af;
            border-radius: 4px;
        }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: {
                            cream: '#FDFBF7',
                            green: '#1A4D2E',
                            terracotta: '#E36414',
                            gold: '#FFB703'
                        }
                    }
                }
            }
        }
    </script>
</head>
<body className="antialiased h-screen flex overflow-hidden">
    <div id="root" className="w-full h-full flex"></div>

    <script type="text/babel">
        const { 
            LayoutDashboard, ShoppingBag, Calendar, Menu: MenuIcon, Clock, Users, Settings, 
            Search, Filter, Plus, Phone, MapPin, X, AlertCircle, CheckCircle2, MoreVertical
        } = lucide;

        // --- Dummy Data ---
        const INITIAL_ORDERS = [
            { id: "STT-0046", status: "Received", customer: "Aishath", phone: "+960 777 1122", total: 120.0, items: "1x Authentic Som Tam", time: "2 min ago", isUrgent: false },
            { id: "STT-0045", status: "Received", customer: "Hassan", phone: "+960 788 3344", total: 310.0, items: "1x Pad Thai, 1x Green Curry", time: "16 min ago", isUrgent: true },
            { id: "STT-0044", status: "Confirmed", customer: "Mariyam", phone: "+960 799 5566", total: 185.0, items: "1x Tom Yum Goong", time: "8 min ago", isUrgent: false },
            { id: "STT-0043", status: "Preparing", customer: "Ali", phone: "+960 711 2233", total: 540.0, items: "3x Pad Kra Pao, 2x Thai Iced Tea", time: "25 min ago", isUrgent: true },
            { id: "STT-0042", status: "Ready", customer: "Ahmed", phone: "+960 789 2616", total: 496.8, items: "2x Authentic Som Tam, 1x Pad Thai", time: "12 min ago", isUrgent: false },
            { id: "STT-0041", status: "Out for Delivery", customer: "Fatima", phone: "+960 722 4455", total: 220.0, items: "1x Massaman Beef", time: "30 min ago", isUrgent: false },
        ];

        const COMPLETED_ORDERS = [
            { id: "STT-0040", customer: "Ibrahim", items: "1x Mango Sticky Rice", total: 110.0, timeCompleted: "11:30 AM" },
            { id: "STT-0039", customer: "Zara", items: "2x Green Curry Chicken", total: 340.0, timeCompleted: "11:15 AM" }
        ];

        const KANBAN_STAGES = ["Received", "Confirmed", "Preparing", "Ready", "Out for Delivery"];

        // --- Shared Sidebar ---
        const AdminSidebar = ({ currentRoute }) => (
            <aside className="bg-brand-green text-white w-64 flex-shrink-0 flex flex-col h-full z-20">
                <div className="p-6 border-b border-white/10 flex items-center gap-3">
                    <div className="w-8 h-8 bg-brand-gold rounded-md flex items-center justify-center text-brand-green font-bold text-xl">S</div>
                    <div>
                        <h1 className="display-font font-bold text-lg leading-tight">Som Tam Thai</h1>
                        <span className="text-[10px] uppercase tracking-widest text-brand-gold font-bold">Admin Panel</span>
                    </div>
                </div>
                <nav className="flex-1 py-6 px-3 space-y-1 overflow-y-auto">
                    {[
                        { id: 'dashboard', icon: 'layout-dashboard', label: 'Dashboard', link: 'dashboard.html' },
                        { id: 'orders', icon: 'shopping-bag', label: 'Orders', badge: '6', link: 'orders.html' },
                        { id: 'reservations', icon: 'calendar', label: 'Reservations', link: 'reservations.html' },
                        { id: 'menu', icon: 'menu', label: 'Menu', link: 'menu.html' },
                        { id: 'schedule', icon: 'clock', label: 'Schedule', link: 'schedule.html' },
                        { id: 'customers', icon: 'users', label: 'Customers', link: 'customers.html' },
                        { id: 'settings', icon: 'settings', label: 'Settings', link: 'settings.html' }
                    ].map(item => (
                        <a key={item.id} href={item.link} className={`flex items-center justify-between px-3 py-2.5 rounded-lg transition-colors ${currentRoute === item.id ? 'bg-white/10 text-brand-gold font-bold' : 'text-gray-300 hover:bg-white/5 hover:text-white'}`}>
                            <div className="flex items-center">
                                <i data-lucide={item.icon} className={`w-5 h-5 mr-3 ${currentRoute === item.id ? 'text-brand-gold' : 'text-gray-400'}`}></i>
                                {item.label}
                            </div>
                            {item.badge && <span className="text-xs font-bold px-2 py-0.5 rounded-full bg-red-500 text-white animate-pulse">{item.badge}</span>}
                        </a>
                    ))}
                </nav>
            </aside>
        );

        // Task 1-5.3: Order Kanban Card Component
        const OrderKanbanCard = ({ order, onAdvance, onCancel }) => {
            const getActionLabel = (status) => {
                switch(status) {
                    case "Received": return "Confirm Order";
                    case "Confirmed": return "Start Preparing";
                    case "Preparing": return "Mark Ready";
                    case "Ready": return "Hand to Rider";
                    case "Out for Delivery": return "Mark Completed";
                    default: return "Next";
                }
            };

            return (
                <div className={`bg-white rounded-lg p-4 shadow-sm mb-3 border-l-4 ${order.isUrgent ? 'border-l-red-500 border border-red-200 ring-1 ring-red-100' : 'border-l-brand-green border border-gray-100'}`}>
                    <div className="flex justify-between items-start mb-2">
                        <span className="font-bold text-gray-900 text-sm">{order.id}</span>
                        <div className="flex items-center">
                            {order.isUrgent && <i data-lucide="alert-circle" className="w-3.5 h-3.5 text-red-500 mr-1 animate-pulse"></i>}
                            <span className={`text-[10px] font-bold px-1.5 py-0.5 rounded ${order.isUrgent ? 'bg-red-50 text-red-600' : 'bg-gray-100 text-gray-500'}`}>{order.time}</span>
                        </div>
                    </div>
                    
                    <div className="mb-3">
                        <div className="font-medium text-sm text-gray-800">{order.customer}</div>
                        <div className="text-xs text-gray-500 flex items-center mt-0.5"><i data-lucide="phone" className="w-3 h-3 mr-1"></i> {order.phone}</div>
                    </div>
                    
                    <div className="text-xs text-gray-600 mb-3 line-clamp-2 bg-gray-50 p-2 rounded">{order.items}</div>
                    
                    <div className="flex justify-between items-center mb-3">
                        <span className="text-xs text-gray-500">Total</span>
                        <span className="font-bold text-brand-green text-sm">MVR {order.total.toFixed(2)}</span>
                    </div>

                    <div className="flex gap-2">
                        <button 
                            onClick={() => onAdvance(order.id)}
                            className="flex-1 bg-brand-green hover:bg-green-900 text-white text-xs font-bold py-2 rounded transition-colors"
                        >
                            {getActionLabel(order.status)}
                        </button>
                        <button 
                            onClick={() => onCancel(order)}
                            className="p-2 border border-gray-200 text-gray-400 hover:text-red-500 hover:border-red-200 hover:bg-red-50 rounded transition-colors"
                            title="Cancel Order"
                        >
                            <i data-lucide="x" className="w-4 h-4"></i>
                        </button>
                    </div>
                </div>
            );
        };

        const App = () => {
            const [orders, setOrders] = React.useState(INITIAL_ORDERS);
            const [showCancelModal, setShowCancelModal] = React.useState(false);
            const [showNewOrderModal, setShowNewOrderModal] = React.useState(false);
            const [selectedOrder, setSelectedOrder] = React.useState(null);

            React.useEffect(() => {
                lucide.createIcons();
            });

            const handleAdvanceOrder = (id) => {
                setOrders(orders.map(o => {
                    if (o.id === id) {
                        const currentIndex = KANBAN_STAGES.indexOf(o.status);
                        if (currentIndex < KANBAN_STAGES.length - 1) {
                            return { ...o, status: KANBAN_STAGES[currentIndex + 1], isUrgent: false };
                        } else {
                            // Completed -> remove from active kanban
                            return { ...o, status: "Completed" };
                        }
                    }
                    return o;
                }));
            };

            const openCancelModal = (order) => {
                setSelectedOrder(order);
                setShowCancelModal(true);
            };

            const activeOrders = orders.filter(o => o.status !== "Completed" && o.status !== "Cancelled");

            return (
                <div className="flex w-full h-full">
                    <AdminSidebar currentRoute="orders" />

                    {/* Modals */}
                    {showCancelModal && (
                        <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
                            <div className="bg-white rounded-xl shadow-xl max-w-md w-full p-6 animate-in zoom-in-95">
                                <h3 className="text-lg font-bold text-gray-900 mb-2">Cancel / Refund Order</h3>
                                <p className="text-sm text-gray-500 mb-4">You are about to cancel {selectedOrder?.id}. Please provide a reason.</p>
                                <textarea className="w-full border border-gray-300 rounded-lg p-3 text-sm focus:border-red-500 focus:outline-none mb-4" rows="3" placeholder="Reason for cancellation..."></textarea>
                                <div className="flex justify-end gap-3">
                                    <button onClick={() => setShowCancelModal(false)} className="px-4 py-2 text-sm font-bold text-gray-600 hover:bg-gray-100 rounded-lg">Close</button>
                                    <button onClick={() => {
                                        setOrders(orders.map(o => o.id === selectedOrder.id ? {...o, status: "Cancelled"} : o));
                                        setShowCancelModal(false);
                                    }} className="px-4 py-2 text-sm font-bold text-white bg-red-600 hover:bg-red-700 rounded-lg">Confirm Cancel</button>
                                </div>
                            </div>
                        </div>
                    )}

                    {showNewOrderModal && (
                        <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
                            <div className="bg-white rounded-xl shadow-xl max-w-lg w-full p-6 max-h-[90vh] overflow-y-auto">
                                <div className="flex justify-between items-center mb-6">
                                    <h3 className="text-xl font-bold text-gray-900">Manual Order Creation</h3>
                                    <button onClick={() => setShowNewOrderModal(false)}><i data-lucide="x" className="w-5 h-5 text-gray-400"></i></button>
                                </div>
                                <div className="space-y-4">
                                    <div className="grid grid-cols-2 gap-4">
                                        <div><label className="text-xs font-bold text-gray-700">Phone Number</label><input type="text" className="w-full border p-2 text-sm rounded mt-1" placeholder="+960 7XX XXXX" /></div>
                                        <div><label className="text-xs font-bold text-gray-700">Customer Name</label><input type="text" className="w-full border p-2 text-sm rounded mt-1" /></div>
                                    </div>
                                    <div><label className="text-xs font-bold text-gray-700">Items</label><textarea className="w-full border p-2 text-sm rounded mt-1" rows="2" placeholder="Item names and quantities"></textarea></div>
                                    <div><label className="text-xs font-bold text-gray-700">Delivery Address</label><textarea className="w-full border p-2 text-sm rounded mt-1" rows="2"></textarea></div>
                                    <div className="grid grid-cols-2 gap-4">
                                        <div>
                                            <label className="text-xs font-bold text-gray-700">Payment</label>
                                            <select className="w-full border p-2 text-sm rounded mt-1 bg-white">
                                                <option>Cash on Delivery</option><option>Bank Transfer</option>
                                            </select>
                                        </div>
                                        <div><label className="text-xs font-bold text-gray-700">Total (MVR)</label><input type="number" className="w-full border p-2 text-sm rounded mt-1" /></div>
                                    </div>
                                    <button onClick={() => setShowNewOrderModal(false)} className="w-full mt-4 bg-brand-green text-white font-bold py-3 rounded-lg">Create Order</button>
                                </div>
                            </div>
                        </div>
                    )}

                    <div className="flex-1 flex flex-col min-w-0 h-full overflow-hidden">
                        
                        {/* Top Filter Bar */}
                        <header className="bg-white border-b border-gray-200 px-6 py-4 flex flex-col sm:flex-row justify-between items-center gap-4 flex-shrink-0 z-10 shadow-sm">
                            <div className="flex items-center gap-2">
                                <div className="w-2 h-2 rounded-full bg-brand-terracotta animate-pulse"></div>
                                <h2 className="text-xl font-bold text-gray-900">Live Orders</h2>
                            </div>
                            
                            <div className="flex flex-wrap items-center gap-3">
                                <div className="relative">
                                    <i data-lucide="search" className="w-4 h-4 text-gray-400 absolute left-3 top-2.5"></i>
                                    <input type="text" placeholder="Search ID or Phone..." className="pl-9 pr-4 py-2 border border-gray-300 rounded-lg text-sm w-48 focus:outline-none focus:border-brand-green" />
                                </div>
                                <button className="flex items-center px-3 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-100">
                                    <i data-lucide="filter" className="w-4 h-4 mr-2"></i> Filters
                                </button>
                                <button onClick={() => setShowNewOrderModal(true)} className="flex items-center px-4 py-2 bg-brand-green text-white rounded-lg text-sm font-bold hover:bg-green-900 shadow-sm">
                                    <i data-lucide="plus" className="w-4 h-4 mr-1"></i> New Order
                                </button>
                            </div>
                        </header>

                        <div className="flex-1 overflow-y-auto p-6 bg-gray-50 flex flex-col">
                            
                            {/* Kanban Board */}
                            <div className="flex gap-4 overflow-x-auto kanban-scroll pb-4 flex-1 items-start">
                                {KANBAN_STAGES.map(stage => {
                                    const stageOrders = activeOrders.filter(o => o.status === stage);
                                    return (
                                        <div key={stage} className="bg-gray-100 rounded-xl p-3 w-80 flex-shrink-0 border border-gray-200 max-h-full flex flex-col">
                                            <div className="flex justify-between items-center mb-3 px-1">
                                                <h3 className="font-bold text-gray-700 uppercase tracking-wider text-xs">{stage}</h3>
                                                <span className="bg-gray-200 text-gray-600 text-xs font-bold px-2 py-0.5 rounded-full">{stageOrders.length}</span>
                                            </div>
                                            
                                            <div className="overflow-y-auto pr-1 flex-1">
                                                {stageOrders.map(order => (
                                                    <OrderKanbanCard 
                                                        key={order.id} 
                                                        order={order} 
                                                        onAdvance={handleAdvanceOrder}
                                                        onCancel={openCancelModal}
                                                    />
                                                ))}
                                                {stageOrders.length === 0 && (
                                                    <div className="border-2 border-dashed border-gray-300 rounded-lg p-6 text-center text-gray-400 text-sm font-medium">
                                                        No orders in {stage}
                                                    </div>
                                                )}
                                            </div>
                                        </div>
                                    );
                                })}
                            </div>

                            {/* Completed Orders Collapsible Table */}
                            <div className="mt-8 bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden flex-shrink-0">
                                <div className="px-6 py-4 border-b border-gray-200 flex justify-between items-center bg-gray-50">
                                    <h3 className="font-bold text-gray-900 flex items-center">
                                        <i data-lucide="check-circle-2" className="w-5 h-5 mr-2 text-gray-400"></i>
                                        Completed Today
                                        <span className="ml-3 bg-white border border-gray-200 text-gray-600 text-xs font-bold px-2 py-0.5 rounded-full">{COMPLETED_ORDERS.length}</span>
                                    </h3>
                                </div>
                                <div className="p-0">
                                    <table className="w-full text-sm text-left text-gray-500">
                                        <thead className="text-xs text-gray-700 uppercase bg-white border-b border-gray-100">
                                            <tr>
                                                <th className="px-6 py-3">Order ID</th>
                                                <th className="px-6 py-3">Customer</th>
                                                <th className="px-6 py-3">Summary</th>
                                                <th className="px-6 py-3 text-right">Total (MVR)</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            {COMPLETED_ORDERS.map((order, i) => (
                                                <tr key={i} className="bg-white border-b border-gray-50 last:border-0 hover:bg-gray-50 transition-colors">
                                                    <td className="px-6 py-4 font-bold text-gray-900 whitespace-nowrap">{order.id}</td>
                                                    <td className="px-6 py-4 font-medium">{order.customer}</td>
                                                    <td className="px-6 py-4 text-xs">{order.items}</td>
                                                    <td className="px-6 py-4 font-bold text-brand-green text-right">{order.total.toFixed(2)}</td>
                                                </tr>
                                            ))}
                                        </tbody>
                                    </table>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
