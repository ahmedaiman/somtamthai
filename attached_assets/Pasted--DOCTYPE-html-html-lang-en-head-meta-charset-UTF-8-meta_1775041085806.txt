<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservations - Admin - Som Tam Thai</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.development.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js" crossorigin></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        /* Design Tokens & Theme File */
        :root {
            --bg-cream: #FDFBF7;
            --primary-green: #1A4D2E;
            --accent-terracotta: #E36414;
            --highlight-gold: #FFB703;
        }
        @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Lora:ital,wght@0,400;0,600;1,400&display=swap');
        
        body { 
            background-color: #f3f4f6; 
            font-family: 'DM Sans', sans-serif; 
            color: #333;
        }
        .display-font { font-family: 'Lora', serif; }
        
        /* Custom scrollbar for calendar grid */
        .calendar-scroll::-webkit-scrollbar { height: 8px; width: 8px; }
        .calendar-scroll::-webkit-scrollbar-track { background: #f1f1f1; border-radius: 4px; }
        .calendar-scroll::-webkit-scrollbar-thumb { background: #c1c1c1; border-radius: 4px; }
        .calendar-scroll::-webkit-scrollbar-thumb:hover { background: #a8a8a8; }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: {
                            cream: '#FDFBF7',
                            green: '#1A4D2E',
                            terracotta: '#E36414',
                            gold: '#FFB703'
                        }
                    }
                }
            }
        }
    </script>
</head>
<body className="antialiased h-screen flex overflow-hidden">
    <div id="root" className="w-full h-full flex"></div>

    <script type="text/babel">
        const { 
            LayoutDashboard, ShoppingBag, Calendar: CalendarIcon, Menu: MenuIcon, Clock, Users, Settings, 
            Search, Filter, Plus, ChevronLeft, ChevronRight, LayoutList, CalendarDays, Edit2, XCircle, CheckCircle, AlertTriangle
        } = lucide;

        // --- Dummy Data ---
        const LUNCH_SLOTS = ['11:30', '12:00', '12:30', '13:00', '13:30', '14:00', '14:30', '15:00'];
        const DINNER_SLOTS = ['18:00', '18:30', '19:00', '19:30', '20:00', '20:30', '21:00'];
        const TABLES = ['Table 1', 'Table 2', 'Table 3'];

        const RESERVATIONS = [
            { id: "RES-001", time: "12:00", name: "Ahmed", phone: "+960 7771122", party: 2, table: "Table 1", status: "Confirmed", notes: "" },
            { id: "RES-002", time: "13:00", name: "Zara", phone: "+960 7883344", party: 4, table: "Table 2", status: "Pending", notes: "Window seat preferred" },
            { id: "RES-003", time: "13:30", name: "Hassan", phone: "+960 7995566", party: 3, table: "Table 1", status: "Confirmed", notes: "High chair needed" },
            { id: "RES-004", time: "18:30", name: "Fatima", phone: "+960 7112233", party: 2, table: "Table 3", status: "No-Show", notes: "" },
            { id: "RES-005", time: "19:00", name: "Ibrahim", phone: "+960 7224455", party: 6, table: "Table 1", status: "Confirmed", notes: "Birthday celebration" },
            { id: "RES-006", time: "19:00", name: "Aishath", phone: "+960 7335577", party: 2, table: "Table 2", status: "Confirmed", notes: "" },
            { id: "RES-007", time: "20:30", name: "Ali", phone: "+960 7446688", party: 4, table: "Table 3", status: "Cancelled", notes: "" },
        ];

        // --- Shared Sidebar ---
        const AdminSidebar = ({ currentRoute }) => (
            <aside className="bg-brand-green text-white w-64 flex-shrink-0 flex flex-col h-full z-20 hidden md:flex">
                <div className="p-6 border-b border-white/10 flex items-center gap-3">
                    <div className="w-8 h-8 bg-brand-gold rounded-md flex items-center justify-center text-brand-green font-bold text-xl">S</div>
                    <div>
                        <h1 className="display-font font-bold text-lg leading-tight">Som Tam Thai</h1>
                        <span className="text-[10px] uppercase tracking-widest text-brand-gold font-bold">Admin Panel</span>
                    </div>
                </div>
                <nav className="flex-1 py-6 px-3 space-y-1 overflow-y-auto">
                    {[
                        { id: 'dashboard', icon: 'layout-dashboard', label: 'Dashboard', link: 'dashboard.html' },
                        { id: 'orders', icon: 'shopping-bag', label: 'Orders', link: 'orders.html' },
                        { id: 'reservations', icon: 'calendar', label: 'Reservations', badge: '1', link: 'reservations.html' },
                        { id: 'menu', icon: 'menu', label: 'Menu', link: 'menu.html' },
                        { id: 'schedule', icon: 'clock', label: 'Schedule', link: 'schedule.html' },
                        { id: 'customers', icon: 'users', label: 'Customers', link: 'customers.html' },
                        { id: 'settings', icon: 'settings', label: 'Settings', link: 'settings.html' }
                    ].map(item => (
                        <a key={item.id} href={item.link} className={`flex items-center justify-between px-3 py-2.5 rounded-lg transition-colors ${currentRoute === item.id ? 'bg-white/10 text-brand-gold font-bold' : 'text-gray-300 hover:bg-white/5 hover:text-white'}`}>
                            <div className="flex items-center">
                                <i data-lucide={item.icon} className={`w-5 h-5 mr-3 ${currentRoute === item.id ? 'text-brand-gold' : 'text-gray-400'}`}></i>
                                {item.label}
                            </div>
                            {item.badge && <span className="text-xs font-bold px-2 py-0.5 rounded-full bg-brand-gold text-brand-green">{item.badge}</span>}
                        </a>
                    ))}
                </nav>
            </aside>
        );

        // --- Task 1-5.5: Calendar View Component ---
        const ReservationCalendarGrid = ({ reservations, onSelectReservation }) => {
            const getStatusColor = (status) => {
                switch(status) {
                    case 'Confirmed': return 'bg-green-100 border-green-300 text-green-800 border-l-4 border-l-green-500';
                    case 'Pending': return 'bg-amber-100 border-amber-300 text-amber-800 border-l-4 border-l-amber-500';
                    case 'No-Show': return 'bg-red-50 border-red-200 text-red-800 border-l-4 border-l-red-500';
                    case 'Cancelled': return 'bg-gray-100 border-gray-200 text-gray-500 border-l-4 border-l-gray-400 opacity-60';
                    default: return 'bg-white border-gray-200';
                }
            };

            const renderRow = (time) => (
                <div key={time} className="flex border-b border-gray-200 min-w-max">
                    <div className="w-24 flex-shrink-0 p-3 bg-gray-50 border-r border-gray-200 font-medium text-gray-600 text-sm flex items-center justify-center">
                        {time}
                    </div>
                    {TABLES.map(table => {
                        const res = reservations.find(r => r.time === time && r.table === table);
                        return (
                            <div key={`${time}-${table}`} className="w-48 flex-shrink-0 p-1.5 border-r border-gray-200 min-h-[80px] bg-white">
                                {res && (
                                    <div 
                                        onClick={() => onSelectReservation(res)}
                                        className={`w-full h-full p-2 rounded shadow-sm border cursor-pointer hover:shadow-md transition-all ${getStatusColor(res.status)} flex flex-col justify-between`}
                                    >
                                        <div>
                                            <div className="font-bold text-sm truncate">{res.name}</div>
                                            <div className="text-xs opacity-80 flex items-center mt-0.5"><i data-lucide="users" className="w-3 h-3 mr-1"></i> {res.party} pax</div>
                                        </div>
                                        <div className="text-[10px] font-bold uppercase tracking-wider mt-1 opacity-80">{res.status}</div>
                                    </div>
                                )}
                            </div>
                        );
                    })}
                </div>
            );

            return (
                <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden flex flex-col h-full">
                    <div className="overflow-x-auto overflow-y-auto calendar-scroll flex-1 relative">
                        {/* Header Row */}
                        <div className="flex border-b border-gray-300 min-w-max sticky top-0 bg-white z-10 shadow-sm">
                            <div className="w-24 flex-shrink-0 p-3 bg-gray-100 border-r border-gray-300"></div>
                            {TABLES.map(table => (
                                <div key={table} className="w-48 flex-shrink-0 p-3 bg-gray-100 border-r border-gray-300 font-bold text-center text-gray-700 text-sm">
                                    {table} <span className="text-xs font-normal text-gray-500 block">(Capacity: 4-6)</span>
                                </div>
                            ))}
                        </div>
                        
                        {/* Lunch Service */}
                        <div className="bg-gray-100 p-2 text-xs font-bold text-gray-500 uppercase tracking-wider sticky left-0 min-w-max border-b border-gray-200">
                            Lunch Service
                        </div>
                        {LUNCH_SLOTS.map(renderRow)}

                        {/* Dinner Service */}
                        <div className="bg-gray-100 p-2 text-xs font-bold text-gray-500 uppercase tracking-wider sticky left-0 min-w-max border-b border-gray-200 mt-2">
                            Dinner Service
                        </div>
                        {DINNER_SLOTS.map(renderRow)}
                    </div>
                </div>
            );
        };

        // --- List View Component ---
        const ReservationListView = ({ reservations, onSelectReservation }) => {
            const getStatusBadge = (status) => {
                switch(status) {
                    case 'Confirmed': return 'bg-green-100 text-green-700';
                    case 'Pending': return 'bg-amber-100 text-amber-700';
                    case 'No-Show': return 'bg-red-100 text-red-700';
                    case 'Cancelled': return 'bg-gray-100 text-gray-500';
                    default: return 'bg-gray-100 text-gray-700';
                }
            };

            return (
                <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                    <div className="overflow-x-auto">
                        <table className="w-full text-sm text-left text-gray-600">
                            <thead className="text-xs text-gray-700 uppercase bg-gray-50 border-b border-gray-200">
                                <tr>
                                    <th className="px-6 py-4">Time</th>
                                    <th className="px-6 py-4">Customer Details</th>
                                    <th className="px-6 py-4 text-center">Party</th>
                                    <th className="px-6 py-4">Table</th>
                                    <th className="px-6 py-4">Status</th>
                                    <th className="px-6 py-4 text-right">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {reservations.map((res, i) => (
                                    <tr key={i} className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
                                        <td className="px-6 py-4 font-bold text-gray-900 whitespace-nowrap">{res.time}</td>
                                        <td className="px-6 py-4">
                                            <div className="font-bold text-gray-900">{res.name}</div>
                                            <div className="text-xs text-gray-500 flex items-center mt-0.5"><i data-lucide="phone" className="w-3 h-3 mr-1"></i>{res.phone}</div>
                                            {res.notes && <div className="text-xs text-brand-terracotta mt-1 truncate max-w-[200px] italic">"{res.notes}"</div>}
                                        </td>
                                        <td className="px-6 py-4 text-center font-medium">{res.party}</td>
                                        <td className="px-6 py-4 font-medium">{res.table}</td>
                                        <td className="px-6 py-4">
                                            <span className={`px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider rounded-full ${getStatusBadge(res.status)}`}>
                                                {res.status}
                                            </span>
                                        </td>
                                        <td className="px-6 py-4 text-right">
                                            <button onClick={() => onSelectReservation(res)} className="text-brand-green hover:text-green-800 font-medium text-sm border border-gray-200 bg-white px-3 py-1.5 rounded shadow-sm hover:shadow transition-all">
                                                Manage
                                            </button>
                                        </td>
                                    </tr>
                                ))}
                            </tbody>
                        </table>
                    </div>
                </div>
            );
        };

        // --- Main App ---
        const App = () => {
            const [viewMode, setViewMode] = React.useState('calendar'); // 'calendar' | 'list'
            const [showCreateModal, setShowCreateModal] = React.useState(false);
            const [selectedReservation, setSelectedReservation] = React.useState(null);

            React.useEffect(() => {
                lucide.createIcons();
            });

            // Calculate Today's Stats
            const activeRes = RESERVATIONS.filter(r => r.status === 'Confirmed' || r.status === 'Pending');
            const totalBookings = activeRes.length;
            const uniqueTablesUsed = new Set(activeRes.map(r => r.table)).size;

            return (
                <div className="flex w-full h-full">
                    <AdminSidebar currentRoute="reservations" />

                    {/* Manage Reservation Modal */}
                    {selectedReservation && (
                        <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
                            <div className="bg-white rounded-xl shadow-xl max-w-md w-full p-6 animate-in zoom-in-95">
                                <div className="flex justify-between items-start mb-6">
                                    <div>
                                        <h3 className="text-xl font-bold text-gray-900">Manage Reservation</h3>
                                        <p className="text-sm text-gray-500">{selectedReservation.id}</p>
                                    </div>
                                    <button onClick={() => setSelectedReservation(null)} className="text-gray-400 hover:text-gray-600"><i data-lucide="x-circle" className="w-6 h-6"></i></button>
                                </div>

                                <div className="bg-gray-50 rounded-lg p-4 mb-6 border border-gray-100">
                                    <div className="grid grid-cols-2 gap-y-4 gap-x-2 text-sm">
                                        <div><span className="block text-xs text-gray-500">Name</span><span className="font-bold">{selectedReservation.name}</span></div>
                                        <div><span className="block text-xs text-gray-500">Phone</span><span className="font-medium">{selectedReservation.phone}</span></div>
                                        <div><span className="block text-xs text-gray-500">Date & Time</span><span className="font-bold text-brand-green">Today, {selectedReservation.time}</span></div>
                                        <div><span className="block text-xs text-gray-500">Details</span><span className="font-medium">{selectedReservation.party} pax • {selectedReservation.table}</span></div>
                                    </div>
                                    {selectedReservation.notes && (
                                        <div className="mt-4 pt-4 border-t border-gray-200">
                                            <span className="block text-xs text-gray-500 mb-1">Notes / Requests</span>
                                            <p className="italic text-gray-700">"{selectedReservation.notes}"</p>
                                        </div>
                                    )}
                                </div>

                                <div className="grid grid-cols-2 gap-3">
                                    <button className="flex items-center justify-center gap-2 bg-green-100 text-green-800 hover:bg-green-200 font-bold py-2.5 rounded-lg transition-colors border border-green-200">
                                        <i data-lucide="check-circle" className="w-4 h-4"></i> Confirm
                                    </button>
                                    <button className="flex items-center justify-center gap-2 bg-gray-100 text-gray-700 hover:bg-gray-200 font-bold py-2.5 rounded-lg transition-colors border border-gray-200">
                                        <i data-lucide="edit-2" className="w-4 h-4"></i> Modify
                                    </button>
                                    <button className="flex items-center justify-center gap-2 bg-red-100 text-red-800 hover:bg-red-200 font-bold py-2.5 rounded-lg transition-colors border border-red-200">
                                        <i data-lucide="x-circle" className="w-4 h-4"></i> Cancel
                                    </button>
                                    <button className="flex items-center justify-center gap-2 bg-amber-100 text-amber-800 hover:bg-amber-200 font-bold py-2.5 rounded-lg transition-colors border border-amber-200">
                                        <i data-lucide="alert-triangle" className="w-4 h-4"></i> No-Show
                                    </button>
                                </div>
                            </div>
                        </div>
                    )}

                    {/* Create Manual Reservation Modal */}
                    {showCreateModal && (
                        <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
                            <div className="bg-white rounded-xl shadow-xl max-w-lg w-full p-6 max-h-[90vh] overflow-y-auto animate-in zoom-in-95">
                                <div className="flex justify-between items-center mb-6">
                                    <h3 className="text-xl font-bold text-gray-900">Create Reservation</h3>
                                    <button onClick={() => setShowCreateModal(false)}><i data-lucide="x" className="w-5 h-5 text-gray-400"></i></button>
                                </div>
                                <div className="space-y-4">
                                    <div className="grid grid-cols-2 gap-4">
                                        <div><label className="text-xs font-bold text-gray-700">Customer Name</label><input type="text" className="w-full border p-2.5 text-sm rounded-lg mt-1 focus:ring-1 focus:ring-brand-green focus:outline-none" /></div>
                                        <div><label className="text-xs font-bold text-gray-700">Phone Number</label><input type="text" className="w-full border p-2.5 text-sm rounded-lg mt-1 focus:ring-1 focus:ring-brand-green focus:outline-none" placeholder="+960 7XX XXXX" /></div>
                                    </div>
                                    <div className="grid grid-cols-3 gap-4">
                                        <div><label className="text-xs font-bold text-gray-700">Date</label><input type="date" className="w-full border p-2.5 text-sm rounded-lg mt-1 bg-white focus:outline-none" defaultValue="2026-04-01" /></div>
                                        <div>
                                            <label className="text-xs font-bold text-gray-700">Time</label>
                                            <select className="w-full border p-2.5 text-sm rounded-lg mt-1 bg-white focus:outline-none">
                                                <option>19:30</option><option>20:00</option>
                                            </select>
                                        </div>
                                        <div><label className="text-xs font-bold text-gray-700">Party Size</label><input type="number" min="1" max="10" defaultValue="2" className="w-full border p-2.5 text-sm rounded-lg mt-1 focus:outline-none" /></div>
                                    </div>
                                    <div><label className="text-xs font-bold text-gray-700">Table Assignment</label>
                                        <select className="w-full border p-2.5 text-sm rounded-lg mt-1 bg-white focus:outline-none">
                                            <option>Auto-assign</option><option>Table 1</option><option>Table 2</option><option>Table 3</option>
                                        </select>
                                    </div>
                                    <div><label className="text-xs font-bold text-gray-700">Notes / Requests</label><textarea className="w-full border p-2.5 text-sm rounded-lg mt-1 focus:outline-none" rows="2"></textarea></div>
                                    
                                    <div className="bg-amber-50 border border-amber-200 rounded-lg p-3 flex items-center justify-between mt-2">
                                        <div>
                                            <div className="text-sm font-bold text-amber-800">Admin Override</div>
                                            <div className="text-xs text-amber-700">Bypass table capacity constraints</div>
                                        </div>
                                        <label className="relative inline-flex items-center cursor-pointer">
                                            <input type="checkbox" className="sr-only peer" />
                                            <div className="w-11 h-6 bg-gray-200 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-amber-500"></div>
                                        </label>
                                    </div>

                                    <button onClick={() => setShowCreateModal(false)} className="w-full mt-6 bg-brand-green hover:bg-green-900 text-white font-bold py-3.5 rounded-lg transition-colors">Confirm & Create</button>
                                </div>
                            </div>
                        </div>
                    )}

                    <div className="flex-1 flex flex-col min-w-0 h-full overflow-hidden">
                        
                        {/* Top Header & Navigation */}
                        <header className="bg-white border-b border-gray-200 px-4 sm:px-6 py-4 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 flex-shrink-0 z-10 shadow-sm">
                            <div className="flex items-center gap-4">
                                <h2 className="text-xl font-bold text-gray-900 hidden sm:block">Reservations</h2>
                                
                                {/* Date Navigator */}
                                <div className="flex items-center bg-gray-50 border border-gray-200 rounded-lg p-1">
                                    <button className="p-1 text-gray-500 hover:text-gray-900 hover:bg-white rounded"><i data-lucide="chevron-left" className="w-5 h-5"></i></button>
                                    <span className="px-4 font-bold text-sm text-gray-900 min-w-[140px] text-center">Today, Apr 1</span>
                                    <button className="p-1 text-gray-500 hover:text-gray-900 hover:bg-white rounded"><i data-lucide="chevron-right" className="w-5 h-5"></i></button>
                                </div>
                            </div>
                            
                            <div className="flex items-center gap-3 w-full sm:w-auto justify-between sm:justify-end">
                                {/* View Toggle */}
                                <div className="flex bg-gray-100 p-1 rounded-lg border border-gray-200">
                                    <button 
                                        onClick={() => setViewMode('calendar')}
                                        className={`flex items-center px-3 py-1.5 text-sm font-medium rounded-md transition-all ${viewMode === 'calendar' ? 'bg-white shadow-sm text-brand-green' : 'text-gray-500 hover:text-gray-900'}`}
                                    >
                                        <i data-lucide="calendar-days" className="w-4 h-4 sm:mr-1.5"></i> <span className="hidden sm:inline">Calendar</span>
                                    </button>
                                    <button 
                                        onClick={() => setViewMode('list')}
                                        className={`flex items-center px-3 py-1.5 text-sm font-medium rounded-md transition-all ${viewMode === 'list' ? 'bg-white shadow-sm text-brand-green' : 'text-gray-500 hover:text-gray-900'}`}
                                    >
                                        <i data-lucide="layout-list" className="w-4 h-4 sm:mr-1.5"></i> <span className="hidden sm:inline">List</span>
                                    </button>
                                </div>

                                <button onClick={() => setShowCreateModal(true)} className="flex items-center px-4 py-2 bg-brand-green text-white rounded-lg text-sm font-bold hover:bg-green-900 shadow-sm transition-colors whitespace-nowrap">
                                    <i data-lucide="plus" className="w-4 h-4 sm:mr-1.5"></i> <span className="hidden sm:inline">New Booking</span>
                                </button>
                            </div>
                        </header>

                        <div className="flex-1 overflow-y-auto p-4 sm:p-6 bg-gray-50 flex flex-col">
                            
                            {/* Today's Stats Bar */}
                            <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6 flex-shrink-0">
                                <div className="bg-white rounded-xl p-4 border border-gray-200 shadow-sm flex items-center justify-between">
                                    <div>
                                        <div className="text-xs text-gray-500 font-bold uppercase tracking-wider mb-1">Tables Utilised</div>
                                        <div className="text-2xl font-bold text-gray-900">{uniqueTablesUsed}/3</div>
                                    </div>
                                    <div className="w-10 h-10 rounded-full bg-brand-green/10 text-brand-green flex items-center justify-center"><i data-lucide="layout-dashboard" className="w-5 h-5"></i></div>
                                </div>
                                <div className="bg-white rounded-xl p-4 border border-gray-200 shadow-sm flex items-center justify-between">
                                    <div>
                                        <div className="text-xs text-gray-500 font-bold uppercase tracking-wider mb-1">Total Bookings</div>
                                        <div className="text-2xl font-bold text-gray-900">{totalBookings}</div>
                                    </div>
                                    <div className="w-10 h-10 rounded-full bg-blue-50 text-blue-600 flex items-center justify-center"><i data-lucide="users" className="w-5 h-5"></i></div>
                                </div>
                            </div>

                            {/* Main View Area */}
                            <div className="flex-1 min-h-0">
                                {viewMode === 'calendar' ? (
                                    <ReservationCalendarGrid reservations={RESERVATIONS} onSelectReservation={setSelectedReservation} />
                                ) : (
                                    <ReservationListView reservations={RESERVATIONS} onSelectReservation={setSelectedReservation} />
                                )}
                            </div>

                        </div>
                    </div>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
